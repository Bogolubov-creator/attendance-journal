{
  "name": "attendance-journal",
  "version": "0.3.1",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.js",
    "dev": "DEMO_MODE=true node --env-file-if-exists=.env src/server.js",
    "test": "node --test tests/*.test.js",
    "format": "prettier --write src public tests package.json README.md DESIGN.md"
  },
  "dependencies": {
    "express": "^5.1.0",
    "openid-client": "^6.8.0"
  },
  "devDependencies": {
    "prettier": "^3.9.6"
  },
  "engines": {
    "node": ">=24"
  }
}
