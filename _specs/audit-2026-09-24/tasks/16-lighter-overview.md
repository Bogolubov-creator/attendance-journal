# 16: Обзор реестра легче порога с запасом

**Статус:** done

**Блокируется:** ничем (можно брать сразу).

**Закрывает критерии:** К14.

## Что построить

Итоговое ревью ветки: обзор на базе «год» весит 299,6 КиБ (306 790 байт) – формально ниже порога 300 КБ только в двоичных единицах, запаса нет. Основной вес строки студента – поля, которые интерфейс реестра не читает целиком: `manager` приходит объектом со всеми участками (scopes), ролью и должностью, а интерфейс берёт только `id` и `name`; `groups` интерфейс не читает вовсе (читает только тест импорта). Ответ обзора (только маршрут обзора, не studentRows – CSV, карточка и проверки прав берут менеджера оттуда) отдаёт `manager` как `{id, name}` и не отдаёт `groups`. Реестр в интерфейсе выглядит и работает как раньше: подпись менеджера, фильтр «Мои», «Менеджер не назначен», фильтр по преподавателю.

## Критерии приёмки

- [x] Сначала падающий тест: в строке обзора `manager` имеет ровно поля `id` и `name` (или null), поля `groups` нет.
- [x] CSV, карточка студента и проверки прав менеджера не изменились (эталон карточек и CSV в tests/overview-snapshot.test.js не меняется; эталон обзора меняется только в manager и groups – показать дифф эталона).
- [x] Тест импорта, читавший `groups` из обзора, переписан на источник, который действительно показывает группы (карточка или связи), а не удалён.
- [x] Замер на базе «год»: обзор меньше 250 000 байт. Число – в сообщении коммита.
- [x] Проверка в браузере: реестр полным доступом и менеджером – подписи менеджера, фильтр «Мои», фильтр по преподавателю работают; pageerror 0.
- [x] Все тесты зелёные, prettier чист.
