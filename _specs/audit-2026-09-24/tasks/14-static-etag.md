# 14: Статика с проверкой свежести

**Статус:** todo

**Блокируется:** ничем (можно брать сразу).

**Закрывает критерии:** К16.

## Что построить

Файлы интерфейса (скрипты, стили, картинки, страницы) отдаются с `Cache-Control: no-cache` и ETag: браузер переспрашивает сервер и при неизменном файле получает 304 без тела. Ответы API и страницы со сканами по-прежнему `no-store`. Остальные заголовки безопасности не меняются.

## Критерии приёмки

- [ ] Сначала падающий тест: запрос скрипта интерфейса отдаёт ETag и `no-cache`; повторный запрос с `If-None-Match` получает 304.
- [ ] Тест: ответ API и отдача скана – `no-store`.
- [ ] CSP, nosniff, запрет встраивания и прочие заголовки безопасности на статике те же, что до правки (сравнить в тесте).
- [ ] Все тесты зелёные, prettier чист.
