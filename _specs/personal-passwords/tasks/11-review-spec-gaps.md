# 11: Хвосты по спеке после второго круга ревью

**Статус:** done

**Блокируется:** ничем (можно брать сразу)

**Закрывает критерии:** К3, К7

## Что построить

Ревью 24.09, круг 2, ось «Спека»:
- К3: список распространённых паролей – около 60 записей вместо «порядка сотни», часть короче 10 символов и никогда не сработает. Нужен список порядка сотни записей длиной от 10 символов.
- К7 и «Как выглядит готовое»: «колонка «Доступ»… на телефоне сворачивается в строку под именем». Сейчас состояние всегда строкой под именем, и на широком экране тоже. На широком экране «Доступ» – отдельная колонка строки, на телефоне – строка под именем.
- README обещает, что неверные попытки не блокируют приглашённого, а неверный код приглашения всё ещё идёт в счётчик. Код около 59 бит онлайн не подобрать, поэтому неверные коды в блокировку не идут; README и код согласованы.

## Критерии приёмки

- [x] Во встроенном списке порядка сотни распространённых паролей, все не короче 10 символов; тест проверяет несколько из них (К3).
- [x] На широком экране состояние доступа – отдельная колонка строки преподавателя, на узком – строка под именем (К7).
- [x] Пять и больше неверных кодов приглашения не закрывают ни «Первый вход», ни вход по паролю.
