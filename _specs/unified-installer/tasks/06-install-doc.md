# 06: INSTALL.md под новый установщик

**Статус:** done

**Блокируется:** 05

**Закрывает критерии:** К15

## Что построить

INSTALL.md описывает новый установщик: запуск `install.sh` / `install.bat`, все сценарии (этот компьютер через Docker и без, удалённый Linux-сервер по SSH), вопросы и ответы по умолчанию, обновление, требования (Node.js 24 на машине, где запускают; Docker или systemd/планировщик на цели; OpenSSH для удалённой установки), свой прокси, первый код, резервные копии и частые ошибки. README ссылается на INSTALL.md; прежние команды упомянуты как совместимые.

## Критерии приёмки

- [x] INSTALL.md покрывает все сценарии, обновление, требования и частые ошибки (К15).
- [x] В тексте только короткое тире; README и DEPLOY.md ссылаются на INSTALL.md.
